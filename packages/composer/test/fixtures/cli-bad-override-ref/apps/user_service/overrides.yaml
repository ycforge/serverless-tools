version: 1
rules:
  - op: add
    target:
      kind: operation
      path: /ping
      method: get
    value:
      summary: '${resources.functions.missing_fn.id}'
      operationId: ping
      x-yc-apigateway-authorizer:
        function_id: '${resources.functions.missing_fn.id}'