openapi: 3.0.0
info:
  title: Reference API Gateway
  version: 1.0.0
  description: Composed entry point over the user_service and analytics reference APIs.
paths:
  /users:
    get:
      summary: List users (user_service API)
      responses:
        "200":
          description: ok
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: ${resources.functions.user_service.id}
        service_account_id: ajefi3b58tak71g3ecp1
  /users/logs:
    get:
      summary: Emit NestJS logs at a chosen level (manual probe)
      parameters:
        - name: level
          in: query
          schema:
            type: string
            enum: [verbose, debug, log, info, warn, error, fatal, all]
            default: log
        - name: message
          in: query
          schema:
            type: string
        # Declared as a string on purpose: Yandex API Gateway materializes an
        # integer-typed parameter default as a JSON number, which the connector
        # rejects fail-fast (every parameter value must be a string → 502).
        # The controller parses and range-checks the value instead.
        - name: count
          in: query
          schema:
            type: string
        - name: context
          in: query
          schema:
            type: string
      responses:
        "200":
          description: ok
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: ${resources.functions.user_service.id}
        service_account_id: ajefi3b58tak71g3ecp1
  /analytics:
    get:
      summary: Analytics health (analytics container API)
      responses:
        "200":
          description: ok
      x-yc-apigateway-integration:
        type: serverless_containers
        container_id: ${resources.containers.analytics.id}
        service_account_id: ajefi3b58tak71g3ecp1
  /analytics/kms:
    get:
      summary: KMS SA-access probe (analytics container)
      responses:
        "200":
          description: ok
      x-yc-apigateway-integration:
        type: serverless_containers
        container_id: ${resources.containers.analytics.id}
        service_account_id: ajefi3b58tak71g3ecp1